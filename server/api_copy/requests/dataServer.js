/**
 * 返回一个完整的数据请求过度
 * @param {object} userParam 用户请求时的实际参数
 * @param {string} actionName 用户请求的action名称
 */
module.exports = function (userParam, actionName) {
    if (Object) {

    }
    // 服务器地址
    const dataServer = 'http://10.0.0.2:89';
    // api说明列表
    /* global sails */
    const dataApi = sails.config.dataApi;


};